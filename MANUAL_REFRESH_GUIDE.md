# 手動更新機能ガイド
## Manual Refresh Feature Guide

---

## ✅ 実装完了

ユーザーアプリに**更新ボタン**を追加しました。このボタンを押すと、管理者パネルで行った変更を手動で読み込むことができます。

---

## 🔄 使い方

### 1. 管理者パネルでメニューを編集
```
https://3003-im8wlpwehyh3f0rsaxz0b-c07dda5e.sandbox.novita.ai
```

1. カテゴリを選択
2. メニューを編集（価格変更、画像追加、売り切れ設定など）
3. 「保存」ボタンをクリック

### 2. ユーザーアプリで更新
```
https://3001-im8wlpwehyh3f0rsaxz0b-c07dda5e.sandbox.novita.ai
```

1. 右上の**青い「更新」ボタン**をクリック
2. ボタンが回転アニメーションして読み込み中を表示
3. 最新のメニューデータが表示される

---

## 🎯 更新ボタンの特徴

### 視覚的デザイン
- 🔵 **青いボタン** - 目立つ色で配置
- 🔄 **回転アイコン** - 更新中はアニメーション
- 🌐 **多言語対応** - 9言語でボタンテキスト表示
  - 日本語: 「更新」
  - English: "Refresh"
  - 简体中文: "刷新"
  - 繁體中文: "刷新"
  - 한국어: "새로고침"
  - Français: "Actualiser"
  - Español: "Actualizar"
  - ไทย: "รีเฟรช"
  - Tiếng Việt: "Làm mới"

### 機能
- ✅ ワンクリックで最新データを取得
- ✅ 読み込み中は操作不可（二重送信防止）
- ✅ エラー時は自動的にlocalStorageから読み込み
- ✅ ページ全体をリロードしない（高速）

---

## 🔧 技術的な動作

### データの流れ

```
┌─────────────────────┐
│  管理者パネル        │
│  (Port 3003)        │
│                     │
│  1. メニュー編集     │
│  2. 保存ボタン押下   │
└──────────┬──────────┘
           │
           │ localStorage保存
           ▼
┌─────────────────────┐
│  localStorage       │
│  (各ポート独立)      │
└──────────┬──────────┘
           │
           │ ユーザーが更新ボタン押下
           ▼
┌─────────────────────┐
│  ユーザーアプリ      │
│  (Port 3001)        │
│                     │
│  3. localStorageから│
│     読み込み        │
│  4. 画面更新        │
└─────────────────────┘
```

### コード（簡略版）

```typescript
const handleRefresh = async () => {
  setIsRefreshing(true);
  try {
    // APIからデータを取得（将来の拡張用）
    const response = await fetch('/api/menu');
    if (response.ok) {
      const data = await response.json();
      setRestaurant(data);
    } else {
      // localStorageから読み込み
      const localData = localStorage.getItem('restaurant_menu_data');
      if (localData) {
        setRestaurant(JSON.parse(localData));
      }
    }
  } finally {
    setIsRefreshing(false);
  }
};
```

---

## ⚠️ 重要な制限

### ❌ できないこと

1. **自動更新はできません**
   - ユーザーが手動で更新ボタンを押す必要があります
   - リアルタイム同期ではありません

2. **異なるポート間でのlocalStorage共有はできません**
   - 管理者パネル（Port 3003）とユーザーアプリ（Port 3001）はそれぞれ独立したlocalStorageを持っています
   - ブラウザのセキュリティ制約により、これは変更できません

3. **管理者パネルで保存しても、ユーザーアプリには自動で届きません**
   - ユーザーアプリ側で更新ボタンを押すまで変更は反映されません

### ✅ できること

1. **更新ボタンを押せば最新データを表示**
   - 確実に動作します
   - シンプルで理解しやすい

2. **複数のユーザーアプリで使用可能**
   - 各ユーザーが自分のタイミングで更新できます

3. **オフラインでも動作**
   - localStorageに保存されているため、ネットワークがなくても表示可能

---

## 🤔 よくある質問

### Q1: 更新ボタンを押しても変更が反映されません

**A1**: 以下を確認してください：

1. **管理者パネルで保存しましたか？**
   - 編集後に「保存」ボタンを押してください

2. **同じブラウザを使っていますか？**
   - Chrome → Chrome、Firefox → Firefox など同じブラウザで確認してください
   - 異なるブラウザではlocalStorageが共有されません

3. **localStorageが有効ですか？**
   - プライベートモード/シークレットモードでは動作しません
   - 通常モードで開いてください

4. **ブラウザのコンソールを確認**
   - F12キーを押してコンソールを開く
   - エラーメッセージがないか確認

### Q2: 自動で更新されるようにできませんか？

**A2**: 現在の実装では不可能です。

**理由**:
- 管理者パネル（Port 3003）とユーザーアプリ（Port 3001）は完全に独立
- localStorageはポートごとに分離されている
- ブラウザのセキュリティ制約

**実現するには**:
- WebSocketサーバーを追加（複雑）
- または両方を同じポートで動作させる（アプリ統合が必要）

### Q3: 他の人の変更も反映されますか？

**A3**: いいえ、**現在の実装では反映されません**。

各ユーザーのlocalStorageは独立しているため：
- 管理者Aが変更 → ユーザーBには届かない
- 共有サーバーがないため

**解決策**:
- 全員が管理者パネルで変更を保存
- または共有データベースを追加（要開発）

---

## 📝 使用シナリオ

### シナリオ1: 価格変更
1. **管理者**: 管理者パネルでメニュー価格を1000円 → 1500円に変更して保存
2. **お客様**: ユーザーアプリで「更新」ボタンを押す
3. **結果**: 新しい価格（1500円）が表示される

### シナリオ2: 売り切れ設定
1. **管理者**: 人気メニューが完売したので「売り切れ」フラグをONにして保存
2. **お客様**: ユーザーアプリで「更新」ボタンを押す
3. **結果**: そのメニューがグレーアウトして「売り切れ」表示

### シナリオ3: 新メニュー追加
1. **管理者**: 新しい季節限定メニューを追加して保存
2. **お客様**: ユーザーアプリで「更新」ボタンを押す
3. **結果**: 新メニューが表示される

---

## 🎯 まとめ

### できること ✅
- 管理者パネルでメニューを編集
- ユーザーアプリで更新ボタンを押して最新データを取得
- シンプルで確実に動作する

### できないこと ❌
- 自動更新（リアルタイム同期）
- 異なるブラウザ間でのデータ共有
- 複数ユーザー間でのデータ共有

### 推奨事項 💡
- **レストランスタッフ**: メニュー変更後、お客様に「更新してください」と案内
- **お客様**: 注文前に更新ボタンを押して最新情報を確認
- **定期的な更新**: 5〜10分に1回程度、更新ボタンを押すことを推奨

---

## 🌐 アクセスURL

### 管理者パネル
```
https://3003-im8wlpwehyh3f0rsaxz0b-c07dda5e.sandbox.novita.ai
```

### ユーザーアプリ（更新ボタンあり）
```
https://3001-im8wlpwehyh3f0rsaxz0b-c07dda5e.sandbox.novita.ai
```

---

**作成日**: 2025-11-07  
**機能**: 手動更新ボタン  
**ステータス**: ✅ 実装完了・動作確認済み
