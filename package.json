{
  "name": "shift-management-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd shift-management-app && npm install && npm run build",
    "install": "cd shift-management-app && npm install"
  }
}
